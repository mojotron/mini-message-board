<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/main.css" />
    <title>Mini Message Board | learn node and express</title>
  </head>
  <body
    class="flex flex-col gap-5 items-center py-8 px-2 bg-gray-100 w-[100vw] h-[100vh]"
  >
    <%- include('../partials/header', { activePage: "messages"}) %>

    <form action="/messages/<%= message.id %>/delete" method="post" class="text-xl text-gray-600 text-center space-y-8 p-8">
      <p>
        Are you sure you want to delete
        <span class="font-bold text-purple-600"><%= message.title %></span>
        message?
      </p>
      <div class="flex justify-center items-center gap-8">
        <!--  -->
        <button type="submit" class="text-xl tracking-wide uppercase font-display rounded-md bg-purple-500 text-gray-50 font-bold px-4 py-1 hover:bg-purple-800 transition-all duration-150">Yes</button>
        <%- include('../partials/innerLink', {linkText:"no",
        linkTo:"/messages/" + message.id })%>
      </div>
    </div>

    <%- include('../partials/footer') %>
  </body>
</html>
